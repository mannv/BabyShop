import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1
  },
  txtClose: {
    justifyContent: 'center',
    textAlign: 'center',
    color: 'red'
  }
})
